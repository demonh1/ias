@model Telecom2.Models.CloseCode

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>CloseCode</legend>

    <div class="display-label">CClose</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CClose)
    </div>

    <div class="display-label">FCClose</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.FCClose)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
